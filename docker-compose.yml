services:
  openfinance-chatbot-backend:
    build:
      context: .
      no_cache: true
      dockerfile: Dockerfile.backend
    image: openfinance-chatbot-image-backend:latest
    ports:
      - "8080:8080"
    restart: always
    container_name: openfinance-chatbot-container
